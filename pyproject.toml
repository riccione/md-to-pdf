[project]
name = "md-to-pdf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown-it-py>=4.2.0",
    "weasyprint>=68.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
